{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for vispotify app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run server\" \"cd ../client && npm start\"",
    "server": "node server.js"
  },
  "author": "Dag G",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0",
    "express": "^4.17.1"
  }
}
